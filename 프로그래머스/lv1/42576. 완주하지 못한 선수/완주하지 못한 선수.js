function solution(participant, completion) {
    let answer = '';
    let name =participant.sort()
    let fin =completion.sort()
    for(let i=0;i<participant.length;i++){
        if(name[i]!=fin[i]){
            answer = name[i]
              return answer; 
        }
    }
}
